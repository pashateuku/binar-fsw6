<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | All Users</title>
</head>
<body>
    <body>
        <% user_game.forEach(i => { %>
          <div>
            <h1><%= i.email %></h1>
            <p><%= i.password %></p>
            <a href="/dashboard/users/<%= i.id %>">Lihat Semua Biodata User</a>
            <a href="/dashboard/users/update/<%= i.id %>">Update Email dan Password</a>
            <a href="/dashboard/users/delete/<%= i.id %>">Hapus User</a>
          </div>
        <% }) %>
</body>
</html>